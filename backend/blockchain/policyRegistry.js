// import { getContract } from "./config.js";

// const policyAddress = process.env.POLICY_REGISTRY_ADDRESS;
// const abiPath = "artifacts/contracts/PolicyRegistry.sol/PolicyRegistry.json";

// export const registerPolicyOnChain = async (policyId, metadataUri) => {
//   const policyRegistry = await getContract(policyAddress, abiPath);
//   const tx = await policyRegistry.registerPolicy(policyId, metadataUri);
//   await tx.wait();
//   return tx.hash;
// };

// !---yet to be implemented --! do not touch this file
